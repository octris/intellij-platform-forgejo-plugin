<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>octris.forgejo</id>
    <name>Forgejo Integration</name>
    <vendor url="https://github.com/octris">octris</vendor>
    <description><![CDATA[
        <p><b>Forgejo Integration</b> brings <a href="https://forgejo.org">Forgejo</a> into JetBrains IDEs.</p>
        <p>It surfaces information from your Forgejo instance directly inside the IDE so you can stay in your editor instead of switching to the browser. Planned and in-progress capabilities include:</p>
        <ul>
            <li>Visualizing <b>Forgejo Actions</b> (CI) status and progress per commit in the VCS log.</li>
            <li>Connecting to self-hosted Forgejo instances via personal access tokens.</li>
        </ul>
        <p>This plugin is under active development.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <!-- Required for the VCS log custom-column extension point. -->
    <depends>com.intellij.modules.vcs</depends>
    <!-- Required for resolving Git remotes to Forgejo owner/repo. -->
    <depends>Git4Idea</depends>

    <resource-bundle>messages.MyBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow factoryClass="octris.forgejo.toolWindow.MyToolWindowFactory" id="MyToolWindow"/>
        <postStartupActivity implementation="octris.forgejo.startup.MyProjectActivity" />

        <!-- Forgejo Integration: accounts settings under Version Control -->
        <projectConfigurable
                parentId="project.propVCSSupport.Mappings"
                id="octris.forgejo.settings"
                displayName="Forgejo Integration"
                instance="octris.forgejo.settings.ForgejoSettingsConfigurable"/>
        <vcsLogCommitStatusProvider implementation="octris.forgejo.vcs.ForgejoCommitStatusProvider"/>
    </extensions>
</idea-plugin>
